/*
#include <stdio.h>

int main(void) {

	int bonbong, bonus, total, tax_rate;
	double tax, real_income;

	printf("급여계산 프로그램(단위: 만원)\n");
	printf("본봉을 입력하세요: ");
	scanf("%d", &bonbong);
	printf("보너스를 입력하세요: ");
	scanf("%d", &bonus);
	printf("공제할 세율(%%)을 입력하세요: ");
	scanf("%d", &tax_rate);


	total = bonbong + bonus;
	tax = (total * (tax_rate * 0.01));
	real_income = total - tax;

	printf("전체 급여에서 공제한 금액은 %lf 입니다.\n", tax);
	printf("실제 수령액은 %lf 입니다.\n", real_income);

	return 0;
}
*/